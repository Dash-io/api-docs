---
openapi: post /deliveries/assign/{rider_uuid}
---










### Create Order

This endpoint allows you to create a new order in the system.

```endpoint
method: POST
path: /deliveries/assign/{{rider_uuid}}

[
  {"label": "curl", "language": "bash", "code": "curl -X POST https://api.example.com/orders -H 'Content-Type: application/json' -d '{\"product\": \"book\", \"quantity\": 1}'"},
  {"label": "Python", "language": "python", "code": "import requests\nresponse = requests.post('https://api.example.com/orders', json={'product': 'book', 'quantity': 1})\nprint(response.json())"},
  {"label": "JavaScript", "language": "javascript", "code": "fetch('https://api.example.com/orders', {\n  method: 'POST',\n  headers: { 'Content-Type': 'application/json' },\n  body: JSON.stringify({ product: 'book', quantity: 1 })\n}).then(response => response.json()).then(data => console.log(data));"},
  {"label": "Java", "language": "java", "code": "HttpRequest request = HttpRequest.newBuilder()\n  .uri(URI.create(\"https://api.example.com/orders\"))\n  .header(\"Content-Type\", \"application/json\")\n  .POST(HttpRequest.BodyPublishers.ofString(\"{\\\"product\\\":\\\"book\\\",\\\"quantity\\\":1}\"))\n  .build();"},
  {"label": "Go", "language": "go", "code": "package main\nimport (\n  \"bytes\"\n  \"net/http\"\n)\nfunc main() {\n  jsonBody := []byte(`{\"product\": \"book\", \"quantity\": 1}`)\n  resp, _ := http.Post(\"https://api.example.com/orders\", \"application/json\", bytes.NewBuffer(jsonBody))\n  defer resp.Body.Close()\n}"}
]
