
### Authorizations

<ResponseField name="Authorization" required header type="string">
  Bearer authentication header of the form Bearer `<store_access_token>`, where `<store_access_token>` is your store token as generated when you created a store.
</ResponseField>  
